spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/task_master_test?createDatabaseIfNotExist=true
    username: root
    password: your_password
#  sql:
#    init:
#      mode: always
  jpa:
    hibernate:
      ddl-auto: update
  config:
    activate:
      on-profile: test
